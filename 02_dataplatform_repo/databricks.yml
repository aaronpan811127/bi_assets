# Boilerplate bundle entrypoint for the shared metric views bundle.

bundle:
  name: shared_metric_views

include:
  - resources/*.yml

variables:
  warehouse_id:
    description: SQL warehouse used to back dashboards and genie spaces.
    default: ""

targets:
  presentation:
    default: true
    mode: production
    workspace:
      host: https://<your-workspace>.cloud.databricks.com
      root_path: /Workspace/Shared/presentation_bi_assets/${bundle.name}
    permissions:
      - level: CAN_MANAGE
        group_name: bi-admins
